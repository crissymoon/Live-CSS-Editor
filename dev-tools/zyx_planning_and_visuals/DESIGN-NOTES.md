## Design Notes

- [] Prrority (low) - the bottom of Crissy's Tool UI remove the bottom white border at the bottm, the windows minimize to a black bar with the tabs but does not extend all the way across the app - make the blk bar on minimize a dark gradient purple 180degress grad - and where it extends all the way across
